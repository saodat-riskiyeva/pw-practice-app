- uses: actions/setup-node@v4
  with:
    node-version: "18"
    cache: "npm"

- name: Pin npm 9 (avoid npm@10 strictness)
  run: |
    corepack disable || true
    npm i -g npm@9
    npm -v

- name: Install dependencies
  run: npm ci
